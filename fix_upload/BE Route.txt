router.post(
  '/',
  authenticate,
  setCustomCreateUploadPath,
  uploadImage.any(), // ✅ Nhận tất cả files (avatar + customFieldFile_*)
  checkImageValidationError,
  parseCustomField,
  validate(employeeValidation.createEmployee),
  employeeController.create,
);